# [App Name Placeholder] Server Code:

This is the flask backend server code for the color web app I have yet to name.

## Notes:

The `extract_colors` function would resolve problem `1` in the list above.
